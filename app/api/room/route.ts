import { NextRequest, NextResponse } from "next/server"

const getRoomId = async (req:NextRequest)=>{
        try {
            
        } catch (error) {
            
        }
}

export {
    getRoomId as GET
}

